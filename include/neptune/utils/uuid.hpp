#ifndef NEPTUNEORM_UUID_HPP
#define NEPTUNEORM_UUID_HPP

#include <string>

namespace neptune::uuid {

std::string uuid(std::size_t length = 36);

} // namespace neptune::uuid

#endif // NEPTUNEORM_UUID_HPP
