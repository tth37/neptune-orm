#ifndef NEPTUNEORM_UTILS_HPP
#define NEPTUNEORM_UTILS_HPP

#include <string>

namespace neptune::utils {

std::string uuid(std::size_t length = 36);

} // namespace neptune::utils

#endif // NEPTUNEORM_UTILS_HPP
