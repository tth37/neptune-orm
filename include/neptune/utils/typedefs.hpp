#ifndef NEPTUNEORM_TYPEDEFS_HPP
#define NEPTUNEORM_TYPEDEFS_HPP

namespace neptune {

enum rel_dir { left = 0, right = 1 };

enum order_dir { asc = 0, desc = 1 };

} // namespace neptune

#endif // NEPTUNEORM_TYPEDEFS_HPP
